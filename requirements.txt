# PlantVest AI Backend — full dependency stack (pinned)
# NOTE: Phase 1 only imports the Core + Flask-extension groups. The AI/PDF/Celery
# deps are declared here per BUILD_SPEC but are not imported until their phases,
# so the app runs (and the health check passes) before they are installed.

# --- Core ---
Flask==3.0.3
python-dotenv==1.0.1
gunicorn==22.0.0

# --- DB / ORM / Migrations ---
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.31
PyMySQL==1.1.1
cryptography==42.0.8

# --- Auth ---
Flask-JWT-Extended==4.6.0
bcrypt==4.1.3

# --- Serialization / Validation ---
flask-marshmallow==1.2.1
marshmallow==3.21.3
marshmallow-sqlalchemy==1.0.0

# --- CORS / Rate limiting ---
Flask-Cors==4.0.1
Flask-Limiter==3.7.0

# --- Async jobs ---
celery==5.4.0
redis==5.0.7

# --- AI / OCR ---
easyocr==1.7.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
torch==2.3.1
torchvision==0.18.1

# --- PDF / QR ---
Jinja2==3.1.4
WeasyPrint==62.3
pydyf==0.10.0            # pinned: pydyf>=0.11 removed Stream.transform, which WeasyPrint 62.3 calls
qrcode==7.4.2

# --- Reporting ---
pandas==2.2.2
openpyxl==3.1.5

# --- Offline ML training charts (ml_training/plot_metrics.py only) ---
# Never imported at request time. Safe to omit from a production install; only
# `python -m ml_training.plot_metrics` needs it, and it says so if absent.
matplotlib==3.9.2

# --- Testing ---
pytest==8.2.2
pytest-flask==1.3.0
