[pytest]
# Tests build the app with the testing config (in-memory SQLite) via the
# fixtures in conftest.py, so no MySQL/Redis instance is required.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
